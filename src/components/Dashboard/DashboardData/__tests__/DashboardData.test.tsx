import React from 'react';
import { View } from 'react-native';
import DashboardData from '../DashboardData';
import { render } from '@testing-library/react-native';

describe('DashboardData component', () => {
  it('renders', () => {
    return false;
  });
});
